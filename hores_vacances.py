'''
CREADOR -> Adria Gracia Sabate
VERSIO --> V1.0
DATA ----> 24/4/2025

jo no dono 0,30€ per a que tu et compris una 0,0 =)

->->->->JUSTIFICACIO DEL CODI CREAT-<-<-<-<-
aquest codi et demana quants dies tens de vacaces i et dona el temps de vacances en hores
'''

print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')
print('|                     VACACHIONI                    |')
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')

print('quants dies de vacances tens en total?')
multi=int(input())
DIA=int(24)

h_totals=DIA*multi
print('les teves hores de vacances son',h_totals,'hores')