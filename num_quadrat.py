'''
CREADOR -> Adria Gracia Sabate
VERSIO --> V1.0
DATA ----> 24/4/2025

jo no dono 0,30€ per a que tu et compris una 0,0 =)

->->->->JUSTIFICACIO DEL CODI CREAT-<-<-<-<-
aquest codi et demana quants dies tens de vacaces i et dona el temps de vacances en hores
'''
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')
print('|                     THE_QUADRATOR                 |')
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')

print('numero a quadrificar?')
num=int(input())
num_final=int(num**2)

print('el quadrat de',num,'es',num_final)