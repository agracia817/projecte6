'''
CREADOR -> Adria Gracia Sabate
VERSIO --> V1.0
DATA ----> 25/4/2025

jo no dono 0,30€ per a que tu et compris una 0,0 =)

->->->->JUSTIFICACIO DEL CODI CREAT-<-<-<-<-
aquest codi imprimeix un saludo personalitzat amb el teu nom
'''
nom=input('introdueix el teu nom: ')


print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')
print('                HOLA',nom)
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')
