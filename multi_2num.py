'''
CREADOR -> Adria Gracia Sabate
VERSIO --> V1.0
DATA ----> 25/4/2025

jo no dono 0,30€ per a que tu et compris una 0,0 =)

->->->->JUSTIFICACIO DEL CODI CREAT-<-<-<-<-
aquest codi imprimeix una multiplicacio a partir de dos numeros demanats
'''
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')
print('|                 LE MULTIPLICATOR                  |')
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')

print('digues el primer numero qu vulguis multiplicar')

primer_num=int(input())

print('digues el seguent valor a multiplicar')
segon_num=int(input())

final_num=primer_num * segon_num

print('_____________________________________________________')
print(final_num)