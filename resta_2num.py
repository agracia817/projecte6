'''
CREADOR -> Adria Gracia Sabate
VERSIO --> V1.0
DATA ----> 25/4/2025

jo no dono 0,30€ per a que tu et compris una 0,0 =)

->->->->JUSTIFICACIO DEL CODI CREAT-<-<-<-<-
aquest codi imprimeix una suma a partir de dos numeros restats
'''
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')
print('|                     LE RESTATOR                   |')
print('<-<-<-<-<-<-<-<-<-<-<-<-<-0->->->->->->->->->->->->->')

print('digues el primer numero qu vulguis restar')

primer_num=int(input())

print('digues el seguent valor a restar')
segon_num=int(input())

final_num=primer_num - segon_num

print('_____________________________________________________')
print(final_num)